import styled from 'styled-components';

function Logo() {
  return (
    <Container>
      my meta data
    </Container>
  );
}

const Container = styled.div`
  font-size: 24px;
  font-weight: bold;
`;

export default Logo;
